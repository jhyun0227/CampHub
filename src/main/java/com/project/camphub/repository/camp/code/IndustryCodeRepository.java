package com.project.camphub.repository.camp.code;

import com.project.camphub.domain.camp.entity.code.IndustryCode;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IndustryCodeRepository extends JpaRepository<IndustryCode, Long> {
}
